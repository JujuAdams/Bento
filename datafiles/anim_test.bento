build BentoRectangle {
	xy = ["50%", "50%"]
	size = [300, 300]

	build BentoRectangle {
		xy = ["50%", "50%"]
		size = [100, 100]

		buildInLength = 300
		buildIn = fn(time) {
			animXOffset = Lerp(1000, 0, time/buildInLength)
		}
	}
}