// Feather disable all

/// @param orientTop
/// @param height
/// @param padding
/// @param drawFunction
/// @param [element=self]

function GuiScrollbarSetHori(_mirror, _thickness, _padding, _drawFunction, _element = self)
{
    if (not GuiExists(_element)) return;
}