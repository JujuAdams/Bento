// Feather disable all

// Juju: This is a common ancestor for some of the example Gui objects. This object in turn
//       inherits from `GuiButtonObject` which is the common Gui button ancestor. This object
//       exists to smooth out some UX issues when updating GML libraries with objects in them that
//       have to be set as parents.

event_inherited();

GuiSetBehavior(GUI_BEHAVIOR_BUTTON);