// This is an example of a completely blank Bento element. Note that we inherit from the same
// object as every other kind of element (`oBentoAncestor`) but this object has no sprite or
// collision mask, nor does it have any user events set up.

//We still have to call any inherited code in this event, however!
event_inherited();